name: C/C++ CI

on:
  [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: mkdir build && cd build && cmake -H../ -B.
    - name: make
      run: make
    - name: test
      run: ctest
